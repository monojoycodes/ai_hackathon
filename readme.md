# How to test this project?
### 1. install python dependencies from all the files.
### 2. get a gemini api key and put it in a file named as `.env`
### 3. run `harmonizer.py` with `python harmonizer.py`
### 4. A `.json` file will be created in the project directory with the harmonized data.
